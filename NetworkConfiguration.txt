#pragma once

// Replace with actual values and change file type to .h

struct NetworkConfiguration
{
	char* ssid = "ssid";
	char* key = "password";
    char* endpointBase = "http://<LapTimerServerIPAddressAndPort>/api/v1/RaceTimer";
};
